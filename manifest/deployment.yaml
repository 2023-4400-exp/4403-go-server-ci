apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: go-server-ci
  name: go-server-ci
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-server-ci
  template:
    metadata:
      labels:
        app: go-server-ci
    spec:
      containers:
        - image: ghcr.io/2023-4400-exp/4403-go-server-ci:f030f5ecf59ce5304d04f7a1fabced120469684a
          name: go-server-ci
